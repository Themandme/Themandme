/**
 * @magnolia/db — Drizzle schema, migrations, typed queries.
 *
 * Populated by BUILD_PLAN M1. M0 establishes the package boundary and its build,
 * lint and type rails only; no domain logic lives here yet.
 */

export const PACKAGE_NAME = '@magnolia/db';
