/**
 * @magnolia/web — Next.js 15 operator dashboard (Today / Pipeline / Deal).
 *
 * Populated by BUILD_PLAN M8. Next.js itself is deliberately NOT a dependency yet: M0 is repo
 * rails, and pulling the framework in early only adds a dependency tree nothing consumes.
 */

export const APP_NAME = '@magnolia/web';
